#include<bits/stdc++.h>

using namespace std;

int main() 
{
  int t;
  vector<int> v;

  while(t--)
  {
     int n;
     cin>>n;

     v.push_back(n);
  }

  sort(v.begin(), v.begin());

  for(int i=0; i<v.size(); i++)
  {
    for(int j=i+1; j<v.size(); j++)
    {
      
    }
  }
}
